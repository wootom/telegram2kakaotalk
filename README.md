# VAAX Telegram to KakaoTalk Forwarder

텔레그램 **vaax notifier** 봇의 메시지를 카카오톡 **VAAX** 채팅방으로 자동 전달하는 시스템입니다.

## 📁 프로젝트 구조

```
vaax-telegram/
├── main.py                    # 메인 엔트리포인트
├── telegram_listener.py       # 텔레그램 메시지 수신 및 처리
├── kakao_sender.py            # 카카오톡 메시지 전송 (AppleScript 기반)
├── config.py                  # API 설정 (API_ID, API_HASH, 채팅방 이름)
├── com.woojanghoon.vaaxtelegram.plist  # macOS LaunchAgent 설정
├── vaax_session.session       # 텔레그램 세션 파일 (자동 생성)
├── service.log                # 서비스 로그 (stdout)
├── service.err                # 서비스 로그 (stderr, 주요 로그)
└── requirements.txt           # Python 의존성
```

## 🔄 동작 과정

```
┌─────────────┐      ┌────────────────────┐      ┌────────────────┐
│  Telegram   │ ──▶  │ telegram_listener  │ ──▶  │  kakao_sender  │
│  vaax bot   │      │    (Python)        │      │  (AppleScript) │
└─────────────┘      └────────────────────┘      └────────────────┘
                              │                          │
                              ▼                          ▼
                     ┌────────────────┐         ┌────────────────┐
                     │ 메시지 필터링   │         │ VAAX 카카오톡  │
                     │ (vaax notifier │         │   채팅방에     │
                     │  에서만 수신)   │         │   메시지 전송  │
                     └────────────────┘         └────────────────┘
```

### 상세 동작 흐름

1. **텔레그램 연결** (`telegram_listener.py`)
   - Telethon 라이브러리로 텔레그램 서버에 연결
   - 사용자 계정으로 로그인 (세션 파일 사용)

2. **메시지 수신 및 필터링**
   - 모든 새 메시지를 수신
   - 발신자 이름에 `vaax notifier`가 포함된 메시지만 처리
   - 나머지 메시지는 무시

3. **카카오톡 전송** (`kakao_sender.py`)
   - 메시지를 클립보드에 복사 (한글/이모지 깨짐 방지)
   - AppleScript로 카카오톡 VAAX 창 활성화
   - 입력 필드 클릭 → Cmd+V 붙여넣기 → Tab + Enter 전송

## ⚙️ 주요 파일 설명

### `config.py`
```python
API_ID = 'YOUR_API_ID'        # Telegram API ID
API_HASH = 'YOUR_API_HASH'    # Telegram API Hash
TARGET_KAKAO_ROOM = 'VAAX'    # 카카오톡 채팅방 이름
```

### `telegram_listener.py`
- 텔레그램 클라이언트 초기화 및 연결
- 메시지 이벤트 핸들러 등록
- `vaax notifier` 필터링 후 `kakao_sender.send_to_kakao()` 호출

### `kakao_sender.py`
- 클립보드 방식으로 한글 메시지 처리 (`pbcopy`)
- AppleScript로 카카오톡 UI 자동화
- `key code 9 using {command down}` = Cmd+V (붙여넣기)
- `key code 48` = Tab, `key code 36` = Enter

### `com.woojanghoon.vaaxtelegram.plist`
- macOS LaunchAgent 설정 파일
- 시스템 시작 시 자동 실행
- 오류 발생 시 자동 재시작 (`KeepAlive: true`)

## 🚀 사용 방법

### 1. 서비스 시작
```bash
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.woojanghoon.vaaxtelegram.plist
```

### 2. 서비스 중지
```bash
launchctl bootout gui/$(id -u)/com.woojanghoon.vaaxtelegram
```

### 3. 서비스 재시작
```bash
launchctl bootout gui/$(id -u)/com.woojanghoon.vaaxtelegram 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.woojanghoon.vaaxtelegram.plist
```

### 4. 로그 확인
```bash
# 실시간 로그 확인
tail -f /Users/woojanghoon/Documents/Code/vaax-telegram/service.err

# 최근 로그 확인
tail -50 /Users/woojanghoon/Documents/Code/vaax-telegram/service.err
```

### 5. 수동 테스트
```bash
cd /Users/woojanghoon/Documents/Code/vaax-telegram
python3 kakao_sender.py "VAAX" "테스트 메시지입니다 🎉"
```

### 6. 서비스 상태 확인
```bash
launchctl list | grep vaax
```

## ⚠️ 필수 조건

### macOS 권한 설정
**시스템 설정 → 개인 정보 보호 및 보안**에서 다음 권한 필요:

| 권한 | 대상 |
|------|------|
| **전체 디스크 접근 권한** | python3, Terminal |
| **손쉬운 사용 (Accessibility)** | python3, Terminal |

### 카카오톡 설정 (⚠️ 중요)

메시지 전송이 정상적으로 작동하려면 아래 조건을 **반드시** 충족해야 합니다:

#### 1. VAAX 단톡방을 별도 창으로 열기
- 카카오톡 메인 창에서 **VAAX** 채팅방을 **더블클릭**하여 별도 창으로 열기
- ⚠️ 메인 창 오른쪽 패널(인라인 모드)이 아닌 **독립된 창**이어야 함

```
✅ 올바른 상태:
┌──────────────┐  ┌──────────────┐
│  카카오톡     │  │    VAAX      │  ← 별도 창
│  메인 창     │  │  (단톡방)    │
└──────────────┘  └──────────────┘

❌ 잘못된 상태:
┌─────────────────────────────────┐
│  카카오톡 메인 창 │    VAAX     │  ← 인라인 모드 (작동 안함)
└─────────────────────────────────┘
```

#### 2. 창 이름 확인
- 창 제목이 정확히 **`VAAX`**여야 함
- 확인 명령어:
  ```bash
  osascript -e 'tell application "System Events" to tell application process "KakaoTalk" to get name of every window'
  ```
- 출력이 `VAAX`를 포함해야 함

#### 3. 화면 배치 조건
- VAAX 창이 **화면에 완전히 보여야** 함 (다른 창에 가려지면 안됨)
- 창이 **최소화되면 안됨**
- 입력 필드가 화면에 보이는 상태여야 함

#### 4. 창 크기 및 위치
- 창 크기가 너무 작으면 클릭 위치가 잘못될 수 있음
- **권장 최소 크기**: 가로 300px 이상, 세로 400px 이상
- 화면 크기 확인:
  ```bash
  osascript -e 'tell application "System Events" to tell application process "KakaoTalk" to tell window "VAAX" to {position, size}'
  ```

#### 5. Mac이 잠금 상태가 아니어야 함
- 화면 보호기 또는 잠금 화면이 활성화되면 UI 자동화가 작동하지 않음
- **에너지 설정**에서 디스플레이 끄기/잠금을 비활성화 권장

#### 6. 입력 필드에 기존 텍스트가 없어야 함 (권장)
- 스크립트가 기존 텍스트를 지우지만, 초기화 실패 시 메시지가 합쳐질 수 있음

## ❓ 자주 묻는 질문 (FAQ)

### Q: 창 위치를 바꿔도 되나요?
**A: 네, 자유롭게 이동 가능합니다.** ✅

코드가 창의 위치와 크기를 동적으로 계산하므로, 창이 화면 어디에 있든 정상 작동합니다:
```applescript
-- 창의 현재 위치와 크기를 자동으로 가져옴
set {vX, vY} to position of vaaxWin
set {vW, vH} to size of vaaxWin

-- 창 위치 기준으로 입력 필드 클릭 좌표 계산
click at {vX + (vW / 2), vY + vH - 80}
```

| 창 위치 | 작동 여부 |
|--------|----------|
| 좌측 상단 | ✅ 작동함 |
| 우측 상단 | ✅ 작동함 |
| 좌측 하단 | ✅ 작동함 |
| 우측 하단 | ✅ 작동함 |
| 화면 중앙 | ✅ 작동함 |

### Q: 창 크기를 변경해도 되나요?
**A: 네, 크기도 자유롭게 변경 가능합니다.** ✅

입력 필드 클릭 위치가 **항상 창 하단에서 80px 위**를 기준으로 계산되므로, 창 높이가 변해도 정상 작동합니다.

| 창 높이 | 입력 필드 클릭 위치 | 작동 여부 |
|--------|-------------------|----------|
| 400px | 하단에서 80px 위 (320px 지점) | ✅ 작동함 |
| 600px | 하단에서 80px 위 (520px 지점) | ✅ 작동함 |
| 800px | 하단에서 80px 위 (720px 지점) | ✅ 작동함 |
| 1000px | 하단에서 80px 위 (920px 지점) | ✅ 작동함 |

- **권장 최소 크기**: 가로 300px × 세로 400px 이상

### Q: 다른 창 뒤로 숨겨져도 되나요?
**A: 네, 자동으로 앞으로 가져옵니다.** ✅

코드에서 `AXRaise` 액션으로 VAAX 창을 자동으로 앞으로 끌어올립니다.

| 상태 | 설명 | 작동 여부 |
|------|------|----------|
| 다른 앱 뒤에 숨겨짐 | Safari, VS Code 등 다른 앱 창 뒤에 있는 경우 | ✅ 자동으로 앞으로 가져옴 |
| Stage Manager로 옆에 숨겨짐 | macOS Stage Manager 사용 시 측면에 숨겨진 경우 | ✅ 자동으로 앞으로 가져옴 |
| 바탕화면 클릭으로 숨겨짐 | 바탕화면 클릭 시 창이 가장자리로 숨겨진 경우 | ✅ 자동으로 앞으로 가져옴 |
| Dock으로 최소화됨 | 노란색 버튼 클릭으로 Dock에 최소화된 경우 | ❌ **작동 안함** |

### Q: 모니터 없이 (헤드리스) 운영할 수 있나요?
**A: 기본적으로는 안됩니다.** ❌

Mac Mini에서 모니터 케이블을 물리적으로 분리하면 화면 좌표 시스템이 없어져 UI 자동화가 작동하지 않습니다.

| 상태 | Mac 인식 | 화면 좌표 | 작동 여부 |
|------|---------|----------|----------|
| 모니터 케이블 연결됨 | 디스플레이 있음 ✅ | 유지됨 | ✅ 작동함 |
| 모니터 케이블 분리됨 | 디스플레이 없음 ❌ | 없음 | ❌ 작동 안함 |

**해결 방법 (모니터 없이 운영하려면):**
1. **HDMI 더미 플러그** 구매 (Amazon에서 "HDMI dummy plug" 검색, 약 5,000~10,000원)
2. 또는 **BetterDummy** 앱으로 가상 디스플레이 생성 (무료, App Store에서 다운로드)

### Q: 모니터 케이블은 연결하고 모니터 전원만 끄면?
**A: 케이블이 연결되어 있으면 작동합니다.** ✅

Mac은 HDMI/DisplayPort 케이블이 물리적으로 연결되어 있으면 **모니터 전원과 무관하게** 디스플레이가 있다고 인식합니다.

| 상태 | Mac 인식 | 화면 좌표 | 작동 여부 |
|------|---------|----------|----------|
| 케이블 연결 + 모니터 전원 ON | 디스플레이 있음 ✅ | 유지됨 | ✅ 작동함 |
| 케이블 연결 + 모니터 전원 OFF | 디스플레이 있음 ✅ | 유지됨 | ✅ 작동함 |
| 케이블 분리 | 디스플레이 없음 ❌ | 없음 | ❌ 작동 안함 |

**⚠️ 모니터 전원 OFF로 장시간 운영 시 필수 설정:**

모니터 전원을 끄고 장시간(밤새 등) 운영하려면 아래 설정을 **반드시** 완료해야 합니다.
설정하지 않으면 Mac이 자동으로 절전/잠금 상태로 진입하여 메시지 전달이 실패합니다.

| 설정 항목 | 경로 | 권장 값 |
|----------|------|--------|
| 디스플레이 끄기 | 시스템 설정 → 에너지 → "디스플레이 끄기" | **안함** |
| 화면 보호기 | 시스템 설정 → 화면 보호기 | **안함** (또는 비활성화) |
| 자동 잠금 | 시스템 설정 → 잠금 화면 → "화면 보호기 시작 후 암호 요구" | **안함** |
| 잠금 화면 전환 | 시스템 설정 → 잠금 화면 → "비활성 후 잠금 화면으로 전환" | **안함** |

**실패 사례:**
```
2026-01-03 08:14:33 - Forwarding to KakaoTalk: VAAX
2026-01-03 08:14:37 - ERROR - Failed to forward message.
```
→ 위 설정을 하지 않아 밤새 Mac이 절전/잠금 상태로 진입하여 아침에 메시지 전달 실패


### Q: 가상 모니터를 쓰면 해상도가 달라서 안되지 않나요?
**A: 아니요, 해상도와 무관하게 작동합니다.** ✅

코드가 창의 위치/크기를 **실시간으로 동적 계산**하므로 해상도가 변경되어도 정상 작동합니다.

| 해상도 | 창 위치 (예시) | 클릭 좌표 계산 | 작동 여부 |
|--------|--------------|---------------|----------|
| 6K (5120×2880, 현재) | (54, 1136) | 동적 계산 | ✅ 작동함 |
| 4K (3840×2160) | (100, 500) | 동적 계산 | ✅ 작동함 |
| 1080p (1920×1080) | (200, 300) | 동적 계산 | ✅ 작동함 |
| 가상 모니터 (1920×1080) | (50, 100) | 동적 계산 | ✅ 작동함 |

**가상 모니터 설정 후 해야 할 일:**
1. 카카오톡 앱 실행
2. VAAX 채팅방을 더블클릭하여 별도 창으로 열기
3. 창이 화면에 보이도록 배치
4. 창 크기가 최소 300×400px 이상인지 확인


## 🔧 문제 해결

### "Operation not permitted" 에러
→ 전체 디스크 접근 권한에 python3 추가

### 한글이 깨질 때
→ `kakao_sender.py`에서 클립보드 + `key code 9` 방식 사용 확인

### 메시지가 안 보내질 때
1. 카카오톡 VAAX 창이 열려있는지 확인
2. 손쉬운 사용 권한 확인
3. 로그 확인: `tail -f service.err`

### 서비스가 재시작 안 될 때
```bash
# 프로세스 강제 종료 후 재시작
pkill -f "python.*main.py"
launchctl bootout gui/$(id -u)/com.woojanghoon.vaaxtelegram 2>/dev/null
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.woojanghoon.vaaxtelegram.plist
```

## 📝 로그 예시

```
2026-01-02 23:19:47 - telegram_listener - INFO - Telegram Client started successfully.
2026-01-02 23:20:15 - telegram_listener - INFO - DEBUG: Sender Username: vaax_fovea_bot
2026-01-02 23:20:15 - telegram_listener - INFO - Resolved Source Name: vaax notifier
2026-01-02 23:20:15 - telegram_listener - INFO - Forwarding to KakaoTalk: VAAX
2026-01-02 23:20:30 - telegram_listener - INFO - Message forwarded successfully.
```

---

**작성일**: 2026-01-02  
**작성자**: Antigravity AI Assistant
